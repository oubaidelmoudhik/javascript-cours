function logBetween(lowNum, highNum) {
  for (let i = lowNum; i <= highNum; i++) {
    console.log(i);
  }
}

// logBetween(1, 10);
// logBetween(25, 31);
// logBetween(-1, 3);
// logBetween(22.5, 27.5);
